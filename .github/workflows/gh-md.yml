name: Compile README

on:
  push:
    branches:
      - main
    paths:
      - 'README.gh.md'
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: uukelele/gh-md@main
